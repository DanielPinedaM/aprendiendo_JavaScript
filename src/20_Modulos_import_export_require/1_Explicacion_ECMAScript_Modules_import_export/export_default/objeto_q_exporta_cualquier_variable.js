const restar = (a, b) => a - b;
function sumar(a, b) {
  return a + b;
}

const numero = 99;

const obj = { saludar: 'hola mundo' };

// exportar
export const array = [1, 2, 3];

// exportar por defecto
export default {
  restar,
  sumar,
  obj,
  numero,
};
